---
name: Prettier-fix
on: [pull_request, push, workflow_dispatch]

permissions:
  contents: write

jobs:
  call-prettier-fix:
    uses: borislav/qubership-workflow-hub/.github/workflows/prettierFix.yaml@main
